type Set @model {
  id: ID!
  title: String!
  company: String
  problems: [Problem] @connection(name: "SetProblems")
  comments: [Comment] @connection(limit: 50, name: "SetComments")
}
type Problem @model {
  id: ID!
  name: String!
  url: String!
  level: String!
  key: String!
  set: Set @connection(name: "SetProblems")
}
type Comment @model {
  id: ID!
  author: String!
  content: String!
  set: Set @connection(name: "SetComments")
}
